export enum OperationType {
  Activate = 'Activate',
  Pause = 'Pause',
  Deactivate = 'Deactivate',
  Reactivate = 'Reactivate',
  Terminate = 'Terminate'
}

export enum MobileNoType {
  iccid = 'icc',
  msisdn = 'msisdn',
  imsi = 'imsi'
}
